# Client Server Architecture

It's Communication Architeture used by HTTP.It has two component one is client and another is server.

Clinet: -many clients
        -use Browser

Server: -one Server
        -placed on specific addres of internet

# Http

It is representful state Transfer protocol. its stateless and cached.

# methods

GET - fetch resources
PUT - create new resource
POST   - Update the resource
PATCH  - update partial resource
DELETE - delete resources

# Catching

Its implement using reverse proxy. The repedatly fetched resource stored in cache for not fetching again from database.

PURGE -its used to delete resource from cache.

